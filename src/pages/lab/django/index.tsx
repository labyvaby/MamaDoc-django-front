import React from "react";
import { Box, Typography } from "@mui/material";

/** Раздел лаборатории. Лента заказов наполняется в следующей задаче. */
const DjangoLabPage: React.FC = () => (
  <Box sx={{ p: 3 }}>
    <Typography variant="h6">Лаборатория</Typography>
  </Box>
);

export default DjangoLabPage;
