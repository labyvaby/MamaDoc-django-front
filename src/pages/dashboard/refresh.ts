/**
 * Автообновление сводки. Экран держат открытым (ресепшен, телефон владельца),
 * и без опроса «сегодня» замирало на моменте открытия. В фоновой вкладке
 * react-query опрос сам останавливает (refetchIntervalInBackground = false).
 *
 * На агрегате (DashboardData.tsx) — по совету бэка из ответа 24.09.2026:
 * разделы «на сейчас» (задачи, воронка, загрузка, брони) — каждые 3 минуты
 * отдельным лёгким запросом (0,07 с), весь экран — реже: полный расчёт
 * тяжелее (≈120 запросов к БД), а касса за период меняется медленнее.
 */
export const LIVE_REFRESH_MS = 3 * 60 * 1000;

/**
 * Полный пересчёт агрегата. ⚠ Интервал — решение фронта, не бэка: бэк
 * советовал полный запрос только при открытии и смене периода, но тогда
 * выручка «сегодня» на весь день застывала бы на моменте открытия.
 */
export const FULL_REFRESH_MS = 10 * 60 * 1000;
